<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

<item android:id="@+id/btn_oval"
    android:icon="@drawable/ic_oval"
    tools:ignore="MenuTitle" />
    <item android:id="@+id/btn_home"
        android:icon="@drawable/ic_hone"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/btn_medal"
        android:icon="@drawable/ic_medal"
        tools:ignore="MenuTitle" />
     <item android:id="@+id/btn_search"
        android:icon="@drawable/ic_search"
        tools:ignore="MenuTitle" />
     <item android:id="@+id/btn_settings"
        android:icon="@drawable/ic_setting"
        tools:ignore="MenuTitle" />


</menu>